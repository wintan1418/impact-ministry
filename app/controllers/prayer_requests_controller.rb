class PrayerRequestsController < ApplicationController
  # Phase 2.7–2.10 will replace this with the real PrayerRequest model + Turbo
  # increments + admin moderation. For now this surface introduces the wall
  # and captures emails so we can notify when it opens.
  def index
  end
end
